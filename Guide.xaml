<Window x:Name="GuideWindow" x:Class="LiveDots.Guide"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LiveDots"
        mc:Ignorable="d"
        Title="Gu&#237;a de Uso" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="9*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0"
            HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <TextBox  Name="guideText" Text="{Binding GuideText}"
                       TextWrapping="Wrap" FontSize="{Binding GuideFontSize}"
                       IsReadOnly="True"
                       IsReadOnlyCaretVisible="True"
                       />
        </ScrollViewer>
        <Button Grid.Row="1" Content="Cerrar" Click="Button_Click" Width="100" HorizontalAlignment="Right" Margin="10,5,10,5"/>
    </Grid>
</Window>
